0x00 什么是TCP协议
     Transmission Control Protocol的缩写，简称传输控制协议。
     TCP是面向连接的，可靠的，基于字节流的。
     
     可靠的：
     TCP采用应答机制
     超时重传
     错误校验
     流量控制和阻塞管理

0x01 TCP通信的步骤
     创建连接
     传输数据
     关闭连接
     
